# Non-sensitive config — committed plaintext. For secrets, see secrets.enc.env
# (SOPS-encrypted with age). deploy.sh combines both into .env at deploy time.

POSTGRES_USER=vision
POSTGRES_DB=vision_template
PORT=3000
NODE_ENV=production
DOMAIN=visiontemplate.watchtoken.org
IMAGE=ghcr.io/allanlacaba/visiontemplate
APP_PORT=3000
BACKUP_KEEP=14
