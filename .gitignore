.DS_Store
.env
/node_modules/

# React Router
/.react-router/
/build/

# Deploy state (lives on the VPS, not in git)
.deploy-state
deploy-failure-*.log

# Real ansible inventory (has live host IPs). Keep inventory.example in git.
ansible/inventory

# Transient compose override (generated by scripts/simulate-bad-deploy.sh)
docker-compose.sim-fail.yml
